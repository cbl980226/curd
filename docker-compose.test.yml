version: '3.9'

services:
  db:
    image: postgres:13
    restart: always
    container_name: test_db
    ports:
      - ${DB_PORT}:5432
    volumes:
      - ${POSTGRES_VOLUME_DIR}:/var/lib/postgresql/data
